# Browser-side API URL (for local dev, override to hit FastAPI directly)
# When running via docker-compose, this is set automatically from SORTING_HAT_API_PORT
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# Server-side API URL (used by Next.js rewrites proxy)
API_URL=http://localhost:8000
